export const metadata = {
    title: 'Retail Description Generator',
    description: '',
  }

export default function CatalogLayout({
    children,
  }) {
    return (
      <div>
        {children}
      </div>
    )
  }