/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["m.media-amazon.com", "mdbproductsbucket.s3.us-east-2.amazonaws.com"]
    }
};

export default nextConfig;
